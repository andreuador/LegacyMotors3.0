{% extends 'base_back.html.twig' %}

{% block title %}Editar empleado{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Editar empleado</h1>

    <div class="my-5">
        {{ include('employee/_form.html.twig', {'button_label': 'Actualizar'}) }}
        <a href="{{ path('app_employee_index') }}" class="btn btn-primary">Volver</a>
    </div>

    {# {{ include('employee/_delete_form.html.twig') }} #}
{% endblock %}
