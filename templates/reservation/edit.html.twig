{% extends 'base_back.html.twig' %}

{% block title %}Editar reserva{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Editar reserva</h1>

    <div class="my-5">
        {{ include('reservation/_form.html.twig', {'button_label': 'Actualizar'}) }}
    </div>

    <a href="{{ path('app_reservation_index') }}" class="btn btn-primary">Volver</a>

    {#{{ include('reservation/_delete_form.html.twig') }}#}
{% endblock %}
