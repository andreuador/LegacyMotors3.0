{% extends 'base_back.html.twig' %}

{% block title %}Reserva{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Reserva</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover text-center">
            <tbody>
            <tr class="d-none">
                <th>Id</th>
                <td>{{ reservation.id }}</td>
            </tr>
            <tr>
                <th>Cliente</th>
                <td>{{ reservation.customer.name }}</td>
            </tr>
            <tr>
                <th>Fecha inicio</th>
                <td>{{ reservation.startDate ? reservation.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Fecha fin</th>
                <td>{{ reservation.endDate ? reservation.endDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Precio total</th>
                <td>{{ reservation.totalPrice }} €</td>
            </tr>
            </tbody>
        </table>
    </div>

    <a href="{{ path('app_reservation_index') }}" class="btn btn-primary">Volver</a>

    <a href="{{ path('app_reservation_edit', {'id': reservation.id}) }}" class="btn btn-success">Editar</a>

    {#{{ include('reservation/_delete_form.html.twig') }}#}
{% endblock %}
