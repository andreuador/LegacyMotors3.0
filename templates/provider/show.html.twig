{% extends 'base_back.html.twig' %}

{% block title %}Proveedor{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Proveedor</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover text-center">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ provider.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ provider.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ provider.email }}</td>
            </tr>
            <tr>
                <th>Dni</th>
                <td>{{ provider.dni }}</td>
            </tr>
            <tr>
                <th>Cif</th>
                <td>{{ provider.cif }}</td>
            </tr>
            <tr>
                <th>Dirección</th>
                <td>{{ provider.address }}</td>
            </tr>
            <tr>
                <th>Movil</th>
                <td>{{ provider.phone }}</td>
            </tr>
            </tbody>
        </table>
    </div>

    <div class="my-3">
        <a href="{{ path('app_provider_index') }}" class="btn btn-primary">Volver</a>
        <a href="{{ path('app_provider_edit', {'id': provider.id}) }}" class="btn btn-success">Editar</a>
    </div>

    {#{{ include('provider/_delete_form.html.twig') }}#}
{% endblock %}
