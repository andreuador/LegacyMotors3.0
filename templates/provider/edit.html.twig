{% extends 'base_back.html.twig' %}

{% block title %}Editar proveedor{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Editar proveedor</h1>

    <div class="my-5">
        {{ include('provider/_form.html.twig', {'button_label': 'Actualizar'}) }}
        <a href="{{ path('app_provider_index') }}" class="btn btn-primary">Volver</a>
    </div>

    {# {{ include('provider/_delete_form.html.twig') }} #}
{% endblock %}
