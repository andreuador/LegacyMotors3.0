{{ form_start(form) }}
<div class="row">
    <div class="col-md-6">
        {{ form_label(form.customer, 'Cliente') }}
        {{ form_widget(form.customer, {'attr': {'class': 'form-control my-2'}}) }}
    </div>
    <div class="col-md-6">
        {{ form_label(form.number, 'Número') }}
        {{ form_widget(form.number, {'attr': {'class': 'form-control my-2'}}) }}
    </div>
</div>
<div class="row">
    <div class="col-md-6">
        {{ form_label(form.date, 'Fecha') }}
        {{ form_widget(form.date, {'attr': {'class': 'form-control my-2'}}) }}
    </div>
    <div class="col-md-6">
        {{ form_label(form.price, 'Precio') }}
        {{ form_widget(form.price, {'attr': {'class': 'form-control my-2'}}) }}
    </div>
</div>
<div class="row">
    <div class="col-12 my-2">
        <button class="btn btn-success">{{ button_label|default('Guardar') }}</button>
    </div>
</div>
{{ form_end(form) }}
