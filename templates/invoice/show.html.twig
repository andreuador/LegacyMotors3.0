{% extends 'base_back.html.twig' %}

{% block title %}Factura{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Factura</h1>

    <div class="table-responsive">
        <table class="table table-striped table-hover text-center">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ invoice.id }}</td>
            </tr>
            <tr>
                <th>Cliente</th>
                <td>{{ invoice.customer.name }}</td>
            </tr>
            <tr>
                <th>Número</th>
                <td>{{ invoice.number }}</td>
            </tr>
            <tr>
                <th>Precio</th>
                <td>{{ invoice.price }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ invoice.date ? invoice.date|date('Y-m-d') : '' }}</td>
            </tr>
            </tbody>
        </table>
    </div>
    <div class="my-3">
        <a href="{{ path('app_invoice_index') }}" class="btn btn-primary">Volver</a>
        <a href="{{ path('app_invoice_edit', {'id': invoice.id}) }}" class="btn btn-success">Editar</a>
    </div>

    {#{{ include('invoice/_delete_form.html.twig') }}#}
{% endblock %}
