{% extends 'base_back.html.twig' %}

{% block title %}Editar cliente{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Editar cliente</h1>

    <div class="my-5">
        {{ include('customer/_form.html.twig', {'button_label': 'Actualizar'}) }}
        <a href="{{ path('app_customer_index') }}" class="btn btn-primary">Volver</a>
    </div>

    {#{{ include('customer/_delete_form.html.twig') }}#}
{% endblock %}
