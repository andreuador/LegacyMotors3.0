{% extends 'base_back.html.twig' %}

{% block title %}Nuevo cliente{% endblock %}

{% block body %}
    <h1 class="font-lm-title my-3">Crear cliente</h1>

    <div class="my-5">
        {{ include('customer/_form.html.twig') }}
        <a href="{{ path('app_customer_index') }}" class="btn btn-primary">Volver</a>
    </div>


{% endblock %}
